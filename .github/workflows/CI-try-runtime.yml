name: Try runtime

run-name: "Workflow CI/CD Steps: execute try-runtime"

on:
  workflow_call:
    inputs:
      execute-try-runtime:
        description: 'Whether to execute try-runtime step or not'
        required: false
        type: string
        default: 'yes'
      try-runtime-version:
        description: 'What version of the try-runtime tool to use'
        required: false
        type: string
        default: 'v0.8.0'
  workflow_dispatch:
    inputs:
      execute-try-runtime:
        description: 'Whether to execute try-runtime step or not'
        required: false
        type: string
        default: 'yes'
      try-runtime-version:
        description: 'What version of the try-runtime tool to use'
        required: false
        type: string
        default: 'v0.8.0'

jobs:
  try-runtime:
    runs-on: warp-ubuntu-latest-x64-8x
    strategy:
      matrix:
        include:
          - runtime: 'mainnet'
            features: ''
            rpc: 'vflow-rpc.zkverify.io'
          - runtime: 'volta'
            features: 'volta'
            rpc: 'vflow-volta-rpc.zkverify.io'
    name: Try runtime ${{ matrix.runtime }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Runtime ${{ matrix.runtime }} build
        uses: ./.github/actions/cmd-in-docker
        with:
          command: "cargo build -p vflow-runtime --release --features try-runtime,${{ matrix.features }}"
          use_cache: 'yes'
          cache_key: 'build-try-runtime-${{ matrix.runtime }}'

      - name: Execute try-runtime ${{ matrix.runtime }}
        if: ${{ inputs.execute-try-runtime == 'yes' }}
        uses: ./.github/actions/cmd-in-docker
        with:
          command: >-
            try-runtime --runtime /build/target/release/wbuild/vflow-runtime/vflow_runtime.compact.compressed.wasm \
              on-runtime-upgrade \
              --blocktime 6 --disable-mbm-checks --disable-spec-version-check live \
              --uri wss://${{ matrix.rpc }}/
          try_runtime: ${{ inputs.try-runtime-version }}
          use_cache: 'no'
